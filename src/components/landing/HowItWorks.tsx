'use client';

export default function HowItWorks() {
  const html = '<section id="how" style="background:var(--deep)">\n  <div class="sec-in">\n    <div class="s-eye">Intelligence Flow</div>\n    <h2 class="sec-title reveal">Data. AI. Alert.<br>In under 8 seconds.</h2>\n    <p class="sec-sub reveal">From anomaly detection to your emergency contacts receiving a ping — the entire response chain completes in less time than a phone call connects.</p>\n    <div class="steps reveal">\n      <div class="step"><div class="step-n">01</div><div class="step-t">Sensor Fusion</div><div class="step-d">All six sensors sample simultaneously at 250Hz. Raw biometric streams are encrypted on-device before any transmission.</div></div>\n      <div class="step"><div class="step-n">02</div><div class="step-t">On-Device AI</div><div class="step-d">A quantized neural network runs inference locally — no cloud latency. Trained on 2.4M cardiac events from clinical datasets.</div></div>\n      <div class="step"><div class="step-n">03</div><div class="step-t">Alert Cascade</div><div class="step-d">Haptic + audio alert to user. Simultaneous push notification to up to 5 emergency contacts with live GPS location and biometrics.</div></div>\n      <div class="step"><div class="step-n">04</div><div class="step-t">Emergency Dispatch</div><div class="step-d">If no user dismissal in 15s, VITAR auto-dials emergency services and transmits a real-time health packet to responding paramedics.</div></div>\n    </div>\n  </div>\n</section>';
  return <div dangerouslySetInnerHTML={{ __html: html }} />;
}
